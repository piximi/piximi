import { makeStyles } from "@mui/material";

export const useStyles = makeStyles((theme) => ({
  grow: {
    flexGrow: 1,
  },
  imageDialogToolboxBar: {
    flexGrow: 1,
    paddingBottom: "50px;",
  },
}));
