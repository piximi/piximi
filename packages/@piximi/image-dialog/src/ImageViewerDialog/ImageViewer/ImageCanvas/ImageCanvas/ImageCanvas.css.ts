import {createStyles} from "@material-ui/core/styles";

export const styles = () =>
  createStyles({
    container: {
      bottom: "16px",
      position: "absolute",
      top: 0
    },
    canvas: {
      marginRight: "300px"
    }
  });
