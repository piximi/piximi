import {createStyles} from "@material-ui/core/styles";

const styles = () =>
  createStyles({
    content: {
      paddingLeft: "12px"
    },
    root: {
      padding: "2px 4px 2px 0",
      display: "flex",
      alignItems: "center"
    }
  });

export default styles;
